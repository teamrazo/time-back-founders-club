# Webhook URL for GHL/Airtable/Make integration (optional)
WEBHOOK_URL=https://hooks.zapier.com/hooks/catch/...

# Directory for saving submission JSON files (optional, defaults to ./data)
# DATA_DIR=/path/to/data
